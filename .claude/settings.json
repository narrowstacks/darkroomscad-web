{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in */public/scad/*.scad) cat <<'JSON'\n{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"permissionDecision\": \"deny\", \"permissionDecisionReason\": \"public/scad/**.scad is generated by sync-scad from the canonical narrowstacks/DarkroomSCAD repo and is overwritten on every build. Do NOT edit it here. Edit the matching file under ../DarkroomSCAD/negative-carriers/ instead, push to narrowstacks/DarkroomSCAD, then run: npm run sync:scad. See CLAUDE.md.\"}}\nJSON\n;; esac",
            "statusMessage": "Checking SCAD edit guard"
          }
        ]
      }
    ]
  }
}
